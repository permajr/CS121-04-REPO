#include <iostream>
using namespace std;

/*int test()
{
	char ch;
	cout << "Enter any string: ";
	cin >> ch;

	if (ch =='1')//== is equality operator while = is assignment operator
	{
		cout << "Just kidding, idiot. Fuck you.";
		return 0;
	}
	else {
		cout << "FIRST character received from the stream: " << ch << endl;

		cin.ignore(0, '#');
		cin.get(ch);
		cout << "SECOND character receied from stream:" << ch << endl;
		ch = cin.peek();
		cout << "char waiting next in stream: " << ch << endl;

		cin.ignore(0, '\n');
		cin.get(ch);
		cout << "THIRD char from stream: " << ch << endl;
		cin.putback(ch);
		cout << "RESTORE char back into stream: " << ch << endl;
		cin.ignore(7, '\n');
		cin.get(ch);
		cout << "FINAL char retrieved from stream: " << ch << endl;



		return 0;
	}

}*/